# WHEN — Environment Configuration
# Copy this file to .env.local and configure as needed

# Application
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Rate Limiting (requests per minute per IP)
RATE_LIMIT_RPM=20
